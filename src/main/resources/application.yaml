ktor:
  application:
    modules:
      - cz.cvut.fit.atlasest.application.ApplicationKt.module
  deployment:
    port: 8080
    host: "127.0.0.1"
    rootPath: "/api"

data:
  collectionsFileName: "db.json"
  identifiersFileName: "identifiers.json"

cors:
  allowed-host: *

pagination:
  defaultLimit: 10
